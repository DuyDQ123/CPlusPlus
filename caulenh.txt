git init tạo repo trên local

git status kiem tra trang thai

git add file_name || git add . dua file vao staging area

git remote add origin https://github.com/DuyDQ123/CPlusPlus.git // Xác định được địa chỉ của remote repo

git push đẩy lên remote repo

git push origin -u main




local -> staging area -> git repo

git branch: List ra các branch local

git branch -M name: tạo branch với cái name đó

git checkout -b name: tạo ra 